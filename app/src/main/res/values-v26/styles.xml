<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.V26.Theme.NoActionBar" parent="Base.Theme.NoActionBar">
        <!-- This attribute is only present on API 27+ but the flag is set in code for 26+ -->
        <!-- <item name="android:windowLightNavigationBar">@bool/light_navigation</item> -->
        <eat-comment/>

        <item name="android:navigationBarColor">@color/navigation_bar</item>
    </style>
    <style name="AppTheme.NoActionBar" parent="Base.V26.Theme.NoActionBar">
    </style>

    <!-- DarkActionBar -->
    <style name="Base.V26.Theme" parent="Base.Theme">
        <!-- This attribute is only present on API 27+ but the flag is set in code for 26+ -->
        <!-- <item name="android:windowLightNavigationBar">@bool/light_navigation</item> -->
        <eat-comment/>

        <item name="android:navigationBarColor">@color/navigation_bar</item>
    </style>
    <style name="AppTheme" parent="Base.V26.Theme">
    </style>
</resources>